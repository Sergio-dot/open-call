{{template "base" .}}

{{define "content"}}

    <!-- Container -->
    <div class="container">

        <div class="row">
            <div class="col">
                <h1>This will be the dashboard page</h1>
                {{if eq .IsAuthenticated 1}}
                    <a href="/user/signout" class="btn btn-outline-danger">Logout</a>
                {{end}}
            </div>
        </div>

    </div>
    <!-- End Container -->

{{end}}